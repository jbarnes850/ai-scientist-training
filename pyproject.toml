[project]
name = "ai-scientist-training"
version = "0.1.0"
description = "Synthetic Prime/verifiers environment for epistemic experiment selection and belief revision"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "verifiers>=0.1.11",
]
